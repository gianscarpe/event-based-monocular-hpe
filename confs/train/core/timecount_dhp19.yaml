core:
  cls: experimenting.dataset.DHP19Core
  params:
    name: timecount_dhp19
    base_path: /data/gscarpellini/dhp19/time_count_dataset
    data_dir: ${dataset.base_path}/movements_per_frame
    hm_dir: ${dataset.base_path}/labels
    labels_dir: ${dataset.base_path}/labels
    joints_dir: ${dataset.base_path}/labels_full_joints
    cams: [0,1,2,3]
    movements:
    preload_dir : ${dataset.base_path}/preload
    n_classes: 33
    n_joints: 13
    partition: cross-view
    test_subjects: [1,2,3,4,5]
    test_cams: [1, 2]        

