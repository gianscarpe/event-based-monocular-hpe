dataset:
  name: timecount_pose
  base_path: /data/dhp19/time_count_dataset
  data_dir: ${dataset.base_path}/movements_per_frame
  hm_dir: ${dataset.base_path}/labels
  joints_dir: ${dataset.base_path}/labels_full_joints
  
  load_split: false
  save_split: false
  split_at: 0.8
  preload_dir : ${dataset.base_path}/preload
  
  test_subjects: [1, 2, 3, 4, 5]
  cams: [3]
  n_channels: 1
  n_joints: 13
